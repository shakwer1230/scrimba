1. typeof 和 instanceof
    typeof 
    判断参数是什么类型 返回几个类型 number string boolean object function  undefined
    可以使用typeof来获取一个变量是否存在 
    if(typeof a != "undefined") return ok;
    直接使用if(a) 如果a未声明（不存在） 则会报错
    但是对于Array Null 类型 typeof 一律返回object

    instanceof
    测试一个对象在原型链中是否存在一个构造函数propertype的属性
    instanceof 用来判断一个变量是否是一个对象的的实列
    instanceof可以在继承关系中用来判断一个实列是否属于他的父类型

2.js的基本六大类型及三大引用类型
    Number String Boolean Undefined Null  使用typeof 变量可以获取
    Object Array  Function 